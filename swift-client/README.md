# Swift Client

iOS and macOS client generated via XcodeGen.

## Setup

```bash
./setup.sh
```

To generate without opening Xcode:

```bash
./setup.sh --no-open
```

## Targets

- vllm-studio (iOS)
- vllm-studio-mac (macOS)
